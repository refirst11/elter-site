---
title: 'style | elter'
subtitle: 'The elter class guide'
date: '2024/06/19'
---

<TocBot />

# elter.style
In style a single syntax is supported. There's no need to think about classNames here.  
we recommend using the variable name style + subject
## Write style and compile 
The hash of the object is directly attached to the className.

<Tabs items={['tsx', 'css' ]}>
  <Tab>
    ```tsx title="Something.tsx"
    import elter from 'elter'

    const styleLink = elter.set({
      fontSize: '14px',
      color: 'var(--color-link)',
      textDecoration: 'none'
    })

    const styleBox = elter.set({
      display: 'block',
      padding: 20,
      width: 60,
      height: 60
    })

    const styleContainer = elter.set({
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center'
    })
    ```
  </Tab>
  <Tab>
    ```css title="css"
    .bbhornk3 {
      font-size: 14px;
      color: var(--color-link);
      text-decoration: none;
    }

    .fhviwm9n {
      display: block;
      padding: 20px;
      width: 60px;
      height: 60px;
    }

    .pb24s9y3 {
      display: flex;
      justify-content: center;
      align-items: center;
    }
    ```
  </Tab>
</Tabs>
