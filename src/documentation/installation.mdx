---
title: 'installation'
subtitle: 'install guid'
date: '2024-06-07'
---

<SearchBox />
<TocBot />

# Installation

### NPM

This page describes installation and build settings, and other details.

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    npm install typedcssx
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    pnpm install typedcssx
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    yarn add typedcssx
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    bun add typedcssx
    ```
  </Tab>
</Tabs>

### Build setting

Add the compile command to the `package.json` script field.

<Tabs items={["npm", "pnpm", "yarn", "bun"]} storageKey="manager">
  {/* prettier-ignore */}
  <Tab>
  ```json title="package.json"
    "scripts": {
      "build": "npm run compile && next build",
      "compile": "cd node_modules/typedcssx && npm run compile"
    },
  ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
  ```json title="package.json"
    "scripts": {
      "build": "npm run compile && next build",
      "compile": "cd node_modules/typedcssx && npm run compile"
    },
  ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
  ```json title="package.json"
    "scripts": {
      "build": "npm run compile && next build",
      "compile": "cd node_modules/typedcssx && npm run compile"
    },
  ```
  </Tab>
    {/* prettier-ignore */}
  <Tab>
  ```json title="package.json"
    "scripts": {
      "build": "npm run compile && next build",
      "compile": "cd node_modules/typedcssx && npm run compile"
    },
  ```
  </Tab>
</Tabs>

#### tsconfig.json
<Tabs>
  <Tab>
  ```json title="tsconfig.json"
    "include": ["**/*css.ts"],
  ```
  </Tab>
</Tabs>

#### Global style sheet
<Tabs>
  <Tab>
```tsx title="layout.tsx"
import '../../node_modules/typedcssx/dist/core/styles/global.css'
```
  </Tab>
</Tabs>

#### Node.js working version

The Node.js version that has been confirmed to work is v18.18.0.  
It has been confirmed that ts-node does not currently work with versions 20 and above.
