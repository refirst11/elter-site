---
title: 'installation'
subtitle: 'install guid'
date: '2024-06-07'
---

import { Tab, Tabs } from '../components/Tabs/index';

# Installation

This page describes installation and build settings, and other details.

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  {/* prettier-ignore */}
  <Tab>
    ```bash
    npm install typedcssx
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash
    pnpm install typedcssx
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash
    yarn add typedcssx
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash
    bun add typedcssx
    ```
  </Tab>
</Tabs>
