---
title: 'Installation'
subtitle: 'The TypedCSSX Installation Guide'
date: '2024-06-07'
---

<TocBot />

# Installation
This page describes installation and compile and plubin, and other details.

## Install packages
<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    npm install typedcssx
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    pnpm install typedcssx
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    yarn add typedcssx
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    bun add typedcssx
    ```
  </Tab>
</Tabs>

<Tabs items={['Next.js', 'Vite', 'Webpack']}>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    pnpm install --save-dev typedcssx-next-plugin
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    under development.
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    pnpm install --save-dev typedcssx-webpack-plugin
    ```
  </Tab>
</Tabs>

## Integration
you need to configure it in your configuration file.
<Tabs items={['Next.js', 'Vite', 'Webpack']}>
  {/* prettier-ignore */}
  <Tab>
    ```js title="next.config.js"
    const { TypedCSSXNextPlugin, configCSSModule } = require('typedcssx-next-plugin')

    /** @type {import('next').NextConfig} */
    const nextConfig = {
      webpack: (config) => {
        config.plugins.push(new TypedCSSXNextPlugin())
        return configCSSModule(config)
      },
    }

    module.exports = nextConfig
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```bash title="shell"
    under development.
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```js title="webpack.config.js"
    const webpack = require("webpack");
    const { TypedCSSXWebpackPlugin } = require("typedcssx-webpack-plugin");

    module.exports = {
      resolve: {
        fallback: {
          fs: false,
          vm: false,
          path: require.resolve("path-browserify"),
          crypto: require.resolve("crypto-browserify"),
          stream: require.resolve("stream-browserify"),
        },
      },
      plugins: [
        new TypedCSSXWebpackPlugin(),
        new webpack.ProvidePlugin({
          process: "process/browser",
          Buffer: ["buffer", "Buffer"],
        }),
      ],
    };
    ```
  </Tab>
</Tabs>

## Compile Command

Add to your build command <code>\`npx typedcssx --compile\`</code>

<Tabs>
  <Tab>
  ```json title="package.json"
    "scripts": {
      "build": "npx typedcssx --compile && next build",
    },
  ```
  </Tab>
</Tabs>

## Server Component
If the anchorEnabled argument is false, you will not be able to see the server component styles unless you manually reload the page once.

but if it is true, you can collect the server component styles by mimicking the behavior of the a tag and reloading the page when transitioning via Link.

This true behavior remembers the page you clicked once and will not reload it thereafter.
```tsx title="layout.tsx"
import { PreviewServerCSS } from 'typedcssx/server'

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html>
      <head>
        <PreviewServerCSS anchorEnabled={true} />
      </head>
      .....
    </html>
  )
}    
```

## Global Style
All you have to do is import the file that calls the cssx.global method.
<Tabs>
  <Tab>
```tsx title="layout.tsx"
import 'styles/global'
```
  </Tab>
</Tabs>

## Supported Node.js

This project has been tested and is confirmed to work with Node.js versions from 18 ~ ^20.  
Please you are using one of these versions for optimal compatibility and performance.  
@types/node version is ^22.5.4. and typescript version is ^5.4.5.

